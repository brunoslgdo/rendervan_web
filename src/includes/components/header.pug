header.header
    include logo
    nav.nav
        ul.menu.hide#menu
            each item in MENU
                li.menu__item: a(href=`${item.href}`, class="menu__link")= `${item.content}`
        .icon-hamburguer#icon-hamburguer: .icon-hamburguer__line